# Copy to /etc/mcp-starter/env on EC2 (chmod 600). Do not commit real values.

OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
MCP_BASE_URL=https://memory-mcp.example.com

# Multi-worker / multi-instance OAuth (FastMCP production — see deploy README)
# JWT_SIGNING_KEY=
# STORAGE_ENCRYPTION_KEY=
# REDIS_URL=redis://your-elasticache:6379/0
